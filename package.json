{
    "scripts": {
        "build": "tsc --build",
        "clean": "tsc --build --clean",
        "test": "jest"
    },
    "dependencies": {
        "jest": "^27.0.6",
        "ts-jest": "^27.0.5"
    },
    "devDependencies": {
        "typescript": "^4.3.5"
    }
}
